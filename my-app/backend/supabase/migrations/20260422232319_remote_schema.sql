-- This file previously contained a full `db pull` / remote schema snapshot. On a database
-- that was already created by earlier migrations, that dump caused duplicate primary keys.
-- The version is kept so remote migration history matches. Add new SQL in new files here.
-- Run `npx supabase db push` from my-app/backend.
SELECT 1;
